# SQL Analytics Layer

This folder contains portfolio-friendly SQL analysis queries designed to run against the BI-ready marts generated by the Python pipeline.

The CSV marts can be loaded into PostgreSQL, SQLite, DuckDB, BigQuery, Snowflake, or Power BI before running equivalent SQL.
